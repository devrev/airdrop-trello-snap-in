{
  "name": "trello-snap-in-conformance-tests",
  "version": "1.0.0",
  "description": "Conformance tests for Trello snap-in incremental data synchronization",
  "scripts": {
    "test": "jest --testTimeout=120000"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^18.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0",
    "axios": "^1.6.0",
    "express": "^4.18.0",
    "@types/express": "^4.17.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/*.test.ts"],
    "setupFilesAfterEnv": ["<rootDir>/jest.setup.js"]
  }
}